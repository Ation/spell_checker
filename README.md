spell_checker
=============
Simple library that provide spell checking for C++ programs.

Search is done among words added to spelling dictionary with specified number of corrections (add or remove symbol).
Set of symbols is configurable by template parameter (look inside testing to get example usage).
